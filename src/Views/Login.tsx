import { View } from "react-native";
import { StyledText, StyledView } from "../Components/StyledComponents";

export default function Login(){
    return(
        <StyledView>
            <StyledText className="text-lg">Login</StyledText>
        </StyledView>
    )
}